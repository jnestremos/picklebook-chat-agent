# Comma-separated keys accepted from clients (same value users enter in the chat dialog).
LLM_PROXY_API_KEYS=dev-test-key,another-key

# Ollama OpenAI-compatible API (not /api/chat).
OLLAMA_BASE_URL=http://127.0.0.1:11434

PORT=3000
